
# /etc/nginx/conf.d/biorisk_ui.conf
sudo tee /etc/nginx/conf.d/biorisk_ui.conf << 'EOF'
location /biorisk-ui/ {
    alias /var/www/html/biorisk-ui/dist/;
    try_files $uri $uri/ /biorisk-ui/dist/index.html;
}
EOF